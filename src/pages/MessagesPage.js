import React from 'react';
import Messages from '../components/Messages';

const MessagesPage = () => {
    return (
        <div>
            <h1>Messages</h1>
            <Messages />
        </div>
    );
};

export default MessagesPage;
